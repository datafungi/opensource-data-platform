all:
  children:
    # All three nodes join as Swarm managers.
    swarm_managers:
      hosts:
        node-1:
          ansible_host: 10.54.1.10
        node-2:
          ansible_host: 10.54.1.11
        node-3:
          ansible_host: 10.54.1.12

    # Stack operations run on node-1 — the primary Swarm manager.
    swarm_primary:
      hosts:
        node-1:
          ansible_host: 10.54.1.10
