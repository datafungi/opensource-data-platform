# cloud-config
package_update: true

runcmd:
  # Install Docker
  - curl -fsSL https://get.docker.com | sh
  - systemctl enable --now docker
  # Add the default Multipass user to the Docker group
  - usermod -aG docker ubuntu